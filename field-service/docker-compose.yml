name: bs-field-service
services:
  bs-field-service:
    container_name: bs-field-service
    #    image: jonathangunawan30/bs-field-service:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8002:8002
    env_file:
      - .env
    depends_on:
      - bs-postgres
    networks:
      - bs-network

  bs-postgres:
    container_name: bs-postgres
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: bs_field_service
    ports:
      - 5432:5432
    networks:
      - bs-network

networks:
  bs-network:
    driver: bridge